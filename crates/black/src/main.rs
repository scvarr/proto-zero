//! BLACK: немой, без логов/памяти/времени.
//! Единственное действие — единичная оценка входа и завершение.
use protozero_core::{run_stdin_forver};

fn main() {
    // Однократно «ощупываем» stdin.
    // Результат намеренно никуда не выводится и не запоминается.
    let _ = run_stdin_forver(|_chunk| {
        // Намеренно ничего не делаем.
    });
}
