diff --git a/crates/black/src/main.rs b/crates/black/src/main.rs
--- a/crates/black/src/main.rs
+++ b/crates/black/src/main.rs
@@ -1,5 +1,5 @@
 //! BLACK: сама сущность без observability.
-//! ADR-026: изменение receptor оставляет первый внутренний динамический след `drive_0`.
+//! ADR-027: оба исхода сравнения receptor причинно изменяют `drive_0`.
 
 use memmap2::MmapOptions;
 use std::{
@@ -29,11 +29,13 @@
 
             if changed {
                 drive_0 += 1.0;
+            } else {
+                drive_0 -= 1.0;
+            }
 
-                // BLACK ничего не публикует. black_box не даёт оптимизатору
-                // удалить немое внутреннее состояние drive_0.
-                let _ = black_box(drive_0);
-            }
+            // BLACK ничего не публикует. black_box не даёт оптимизатору
+            // удалить немое внутреннее состояние drive_0.
+            let _ = black_box(drive_0);
         }
 
         previous = Some(current);
diff --git a/crates/white/src/main.rs b/crates/white/src/main.rs
--- a/crates/white/src/main.rs
+++ b/crates/white/src/main.rs
@@ -1,5 +1,5 @@
 //! WHITE: анатомический двойник BLACK + внешняя лабораторная instrumentation.
-//! ADR-026: обнаруженное изменение увеличивает внутреннюю координату `drive_0` на `1.0`.
+//! ADR-027: различие сдвигает `drive_0` на `+1.0`, равенство — на `-1.0`.
 
 mod metrics;
 
@@ -43,6 +43,8 @@
 
             if changed {
                 drive_0 += 1.0;
+            } else {
+                drive_0 -= 1.0;
             }
 
             metrics::record_comparison(previous_value, current, changed, drive_0);
diff --git a/docs/00-core/physical-digital-substrate.md b/docs/00-core/physical-digital-substrate.md
--- a/docs/00-core/physical-digital-substrate.md
+++ b/docs/00-core/physical-digital-substrate.md
@@ -123,9 +123,9 @@
 - не назначается человеческая семантика без основания;
 - новая структура должна причинно опираться на уже существующую анатомию либо вводить ровно тот новый элемент, который исследуется.
 
-## Текущая точка ADR-026
+## Текущая корневая линия
 
-После ADR-026 собственное состояние Black:
+После ADR-027 собственное состояние Black по-прежнему содержит только:
 
 ```text
 previous: Option<u8>
@@ -138,22 +138,23 @@
 receptor
 → current
 → compare with previous
-→ changed
-→ if changed: drive_0 += 1.0
+→ Different / Equal
+→ Different: drive_0 += 1.0
+→ Equal:     drive_0 -= 1.0
 → previous = current
 ```
 
-Значение `drive_0` уже участвует в вычислении собственного следующего значения как левый операнд сложения.
+`drive_0` участвует в вычислении собственного следующего значения и теперь способен двигаться в обе стороны без добавления новой внутренней координаты.
 
-Но на текущей стадии оно:
+На текущей стадии он:
 
-- не влияет на обнаружение `changed`;
+- не влияет на результат сравнения;
 - не влияет на receptor;
 - не влияет на `previous` иначе, чем через общую последовательность цикла;
 - не влияет на другую внутреннюю координату, потому что её нет;
 - не влияет на motor output, потому что его нет;
 - не влияет на World.
 
-Следовательно, `drive_0` уже является сохраняющейся внутренней динамической координатой, но пока не является модулятором другой причинной функции агента.
+Продолжение causal loop теперь само причинно значимо: повторное наблюдение равного receptor-state изменяет `drive_0`.
 
-У агента также нет внутреннего понятия времени. Поэтому рассуждения о скорости насыщения, замедлении «со временем» или временном decay не являются свойствами текущей анатомии.
+Это не вводит внутренний clock или единицу внешнего времени. Конкретная траектория зависит от реальной скорости исполнения процесса и последовательности receptor-state, которую процесс фактически успевает наблюдать.
diff --git a/docs/27-adr/ADR-027-symmetric-comparison-consequence.md b/docs/27-adr/ADR-027-symmetric-comparison-consequence.md
new file mode 100644
--- /dev/null
+++ b/docs/27-adr/ADR-027-symmetric-comparison-consequence.md
@@ -0,0 +1,141 @@
+# ADR-027: Symmetric Consequence of Comparison
+
+**Статус:** implemented, laboratory verification pending  
+**Дата:** 2026-08-18  
+**Связанные:** [ADR-026](../26-adr/ADR-026-first-internal-drive.md), [Physical Digital Substrate and Evolution Triggers](../00-core/physical-digital-substrate.md)
+
+## Основание
+
+ADR-026 использует результат уже существующего сравнения:
+
+```text
+current != previous
+```
+
+но причинное последствие получает только один исход:
+
+```text
+different → drive_0 += 1.0
+equal     → no internal change
+```
+
+Текущая анатомия работоспособна; это не intrinsic crisis.
+
+ADR-027 является developmental step: не вводя нового сенсора, памяти или внутренней координаты, сделать причинно значимым второй уже существующий исход сравнения.
+
+## Решение
+
+Первое чтение по-прежнему только устанавливает baseline:
+
+```text
+previous = None
+current = read(receptor)
+
+→ drive_0 не изменяется
+→ previous = current
+```
+
+Для каждого последующего чтения:
+
+```text
+current = read(receptor)
+
+if current != previous:
+    drive_0 += 1.0
+else:
+    drive_0 -= 1.0
+
+previous = current
+```
+
+Оба смещения симметричны по модулю и используют уже установленную фундаментальную внутреннюю единицу `1.0`. Фактический представимый следующий state определяется физическим `f64`.
+
+## Что означает `Equal`
+
+`Equal` означает только `current == previous` для двух последовательных чтений receptor самим агентом.
+
+Это не означает, что World между чтениями не изменял receptor. World может выполнить `42 → 17 → 42`, а агент при двух своих чтениях увидеть `42 → 42` и получить `Equal`.
+
+Следовательно, внутренняя динамика зависит от последовательности состояний, реально наблюдённых процессом агента, а не от полной лабораторной истории World.
+
+## Семантика `drive_0` после ADR-027
+
+`drive_0` больше нельзя интерпретировать просто как количество обнаруженных изменений.
+
+Минимальная операционная семантика:
+
+> сохраняющаяся внутренняя координата накопленного баланса между последовательными наблюдениями различия и равенства receptor-state.
+
+Положительная и отрицательная области не получают семантики «хорошо / плохо», «возбуждение / покой» или «желательно / нежелательно». Ноль остаётся только центром координаты.
+
+## Собственный ход процесса
+
+После ADR-027 повторное наблюдение того же значения тоже изменяет внутреннее состояние.
+
+Это не вводит clock, timestamp, duration или единицу внешнего времени. Агент не знает, сколько физического времени прошло.
+
+Конкретная траектория зависит от реально выполненной последовательности собственных циклов и от состояний receptor, увиденных в этих циклах. Поэтому она также зависит от физического субстрата исполнения процесса.
+
+## White и Black
+
+White и Black имеют один и тот же причинный закон, но не обязаны иметь одинаковые численные траектории `drive_0`.
+
+Instrumentation White может менять скорость его исполнения. Это является отличием физического субстрата конкретного запуска, а не отличием анатомического закона.
+
+World остаётся лабораторной установкой и может использоваться для создания сопоставимых относительных режимов внешней динамики для Black и White.
+
+## Латентная структура `NotEqual`
+
+Для двух `u8` доступны три отношения:
+
+```text
+current < previous
+current = previous
+current > previous
+```
+
+Текущий закон причинно различает только два класса:
+
+```text
+Equal
+NotEqual = Less OR Greater
+```
+
+Оба подслучая `NotEqual` пока имеют одно и то же последствие:
+
+```text
+Less    → +1.0
+Greater → +1.0
+```
+
+ADR-027 не материализует `Less` и `Greater` как отдельные причинные ветви Black и не назначает им различную семантику.
+
+Если в будущем появится основание обрабатывать направления различия по-разному, необходимая информация уже содержится в `current` и `previous`.
+
+## Что не вводится
+
+ADR-027 не вводит threshold, reset, clamp, saturation law, decay, стремление к нулю, clock, timer, второй drive, motor, новый sensor или отдельное состояние для `Less` / `Greater`.
+
+## Acceptance criteria
+
+1. Первое чтение receptor только устанавливает `previous` и не изменяет `drive_0`.
+2. При `current != previous` выполняется `drive_0 += 1.0`.
+3. При `current == previous` выполняется `drive_0 -= 1.0`.
+4. Black и White используют одинаковый причинный закон.
+5. Новые state variables не вводятся.
+6. `Less` и `Greater` не получают разных причинных последствий.
+7. Фактический результат арифметики определяется физическим `f64`.
+8. Никакой временной единицы, threshold, reset или regulation не вводится.
+
+## Лабораторная проверка
+
+Минимальная проверка подтверждает обе ветви закона:
+
+```text
+different → drive_0 increases by one representable internal unit
+equal     → drive_0 decreases by one representable internal unit
+```
+
+Ручного управления World достаточно для функциональной проверки ADR-027.
+
+Исследование статистической динамики при автономном хаотическом World является отдельным следующим лабораторным этапом.
diff --git a/docs/README.md b/docs/README.md
--- a/docs/README.md
+++ b/docs/README.md
@@ -3,6 +3,7 @@
 ## Current status
 - Current root line: [ADR-025 — Change Detection / First Private Memory](./25-adr/ADR-025-change-detection-first-memory.md) is implemented and laboratory-verified.
 - [ADR-026 — First Internal Dynamic Coordinate (`drive_0`)](./26-adr/ADR-026-first-internal-drive.md) is implemented and laboratory-verified.
+- [ADR-027 — Symmetric Consequence of Comparison](./27-adr/ADR-027-symmetric-comparison-consequence.md) is implemented; laboratory verification pending.
 
 ## Core docs
 - [Physical Digital Substrate and Evolution Triggers](./00-core/physical-digital-substrate.md)
@@ -33,6 +34,7 @@
 - [ADR-021](./21-adr/ADR-021-configurable-world.md)
 - [ADR-025](./25-adr/ADR-025-change-detection-first-memory.md)
 - [ADR-026](./26-adr/ADR-026-first-internal-drive.md)
+- [ADR-027](./27-adr/ADR-027-symmetric-comparison-consequence.md)
 
 ## Future ADRs
 - [ADR-016](./future/ADR-016-event-based-decay.md)
diff --git a/docs/roadmap/ADR-ROADMAP.md b/docs/roadmap/ADR-ROADMAP.md
--- a/docs/roadmap/ADR-ROADMAP.md
+++ b/docs/roadmap/ADR-ROADMAP.md
@@ -1,6 +1,7 @@
 ﻿# ADR Roadmap — Proto-Zero → Proto-One
 
-Каждый ADR — не просто техническое решение, а фиксация осознанного перехода системы через кризис.
+Каждый ADR — не просто техническое решение, а фиксация осознанного причинного перехода системы.
+Основанием перехода может быть intrinsic crisis или минимальный developmental step согласно core-принципам.
 Развитие агента происходит через эволюционные ступени:
 
 1. **Эмергенция** — появление drive и саморегуляции.
@@ -11,7 +12,7 @@
 6. **Партнёрство** — целевая фаза: устойчивое взаимодействие с человеком.
 
 Каждый новый ADR должен:
-- ссылаться на кризис предыдущего;
+- явно фиксировать основание перехода: intrinsic crisis или developmental step;
 - фиксировать новые сущности строго минимально;
 - не вводить параметры, если их нельзя вывести;
 - сохранять симметрию black/white;
@@ -23,12 +24,15 @@
 
 [ADR-026](../26-adr/ADR-026-first-internal-drive.md) продолжает эту корневую линию: обнаруженное изменение уже существует, но после сравнения не оставляет собственного динамического следа, кроме служебного обновления `previous`. Минимальное решение — одна внутренняя координата `drive_0`.
 
+[ADR-027](../27-adr/ADR-027-symmetric-comparison-consequence.md) развивает уже существующее сравнение без новых state variables: `NotEqual` сдвигает `drive_0` на `+1.0`, `Equal` — на `-1.0`.
+
 Текущая линия после переоценки:
 
 | ADR | Суть | Статус |
 | --- | --- | --- |
 | 025 | Изменение внешней ячейки → `previous` → эфемерный `changed` | ✅ реализовано и проверено |
 | 026 | `changed` → первая внутренняя динамическая координата `drive_0` | ✅ реализовано и проверено |
+| 027 | Оба исхода сравнения получают симметричное последствие `+1.0 / -1.0` | реализовано, проверка ожидается |
 
 Пока на этой точке нет параллельных кризисов, развитие продолжается по `main`. Git-ветка создаётся только при реальной развилке: несколько независимых кризисов или несколько несовместимых способов пройти один кризис. Каждая такая ветка может исследоваться до результата или тупика и не обязана сливаться обратно.
 
